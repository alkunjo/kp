<%= form_for @pabrik, remote: true, html: {style: "display:inline;"} do |f| %>
    <% if @pabrik.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@pabrik.errors.count, "error") %> prohibited this jenis obat from being saved:</h2>

        <ul>
        <% @pabrik.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <div class="col-md-3">
        <%= f.label :nama_pabrik, class: "control-label" %><br>
      </div>
      <div class="col-md-5">
        <%= f.text_field :pabrik_name, class: "form-control" %>
      </div>
      <div class="col-md-2">
        <%= f.submit (f.object.new_record? ? "Tambah" : "Ubah"), class: "btn btn-sm btn-primary" %>
      </div>
      <div class="col-md-1">
        <%= link_to 'Batal', new_pabrik_path, class: "btn btn-sm btn-default", remote: true %>
      </div>
    </div>
<% end %>