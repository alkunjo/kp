<%= form_for([@transaksi_ask, @dtrans_ask], remote: true) do |f| %>
  <% if @dtrans_ask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dtrans_ask.errors.count, "error") %> prohibited this dtrans_ask from being saved:</h2>

      <ul>
      <% @dtrans_ask.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-2">
      <%= f.label :kuantitas %>
    </div>
    <div class="col-md-2">
      <%= f.number_field :dta_qty, class: "form-control" %>
    </div>
    <div class="col-md-2">
      <%= f.label :obat %>
    </div>
    <div class="col-md-4">
      <%= f.text_field :obat_id, class: "form-control" %>
    </div>
    <div class="col-md-2">
      <%= f.submit %>
    </div>
  </div>
<% end %>
