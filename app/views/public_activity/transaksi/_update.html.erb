<a href="#" class="list-group-item" style="background-color: #63a1d5; color: #ffffff">
<i class="fa fa-edit fa-fw">
</i>&nbsp;&nbsp;<b><%= User.find(activity.owner).user_name %></b> dari <b>Apotek <%= Outlet.find(activity.trackable.sender_id).outlet_name %></b> 
 telah mengubah permintaan obat ke  <b>Apotek <%= Outlet.find(activity.trackable.receiver_id).outlet_name %></b>
<span class="pull-right text-muted-small">
	<em><%= activity.trackable.updated_at.strftime("%d %B %Y") %></em>
</span>
</a>