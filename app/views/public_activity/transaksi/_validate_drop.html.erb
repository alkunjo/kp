<a href="#" class="list-group-item">
<i class="fa fa-arrow-circle-down fa-fw">
</i>&nbsp;&nbsp;<b><%= User.find(activity.owner).user_name %></b> dari <b>Apotek<%= Outlet.find(activity.trackable.receiver_id).outlet_name %></b> 
telah memvalidasi dropping obat ke <b>Apotek <%= Outlet.find(activity.trackable.sender_id).outlet_name %></b>
<span class="pull-right text-muted-small">
	<em><%= activity.trackable.dropped_at.strftime("%d %B %Y") %></em>
</span>
</a>