<% @roles.each do |role| %>
  <tr>
  	<td><%= role.role_name %></td>
  	<td><%= role.role_desc %></td>
    <td><%= link_to 'Hapus', del_role_path(role), class: "btn btn-danger btn-sm", remote: true, html:{style: "color:#FFFFFF"} %></td>
  </tr>
<% end %>